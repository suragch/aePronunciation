<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
      xmlns:app="http://schemas.android.com/apk/res-auto">

    <item android:id="@+id/action_copy"
          android:title="@string/keyboard_menu_copy"
          android:icon="@mipmap/ic_content_copy_white_24dp"
          app:showAsAction="ifRoom"/>
    <item android:id="@+id/action_clear"
          android:title="@string/keyboard_menu_clear"
          android:icon="@mipmap/ic_clear_white_24dp"
          app:showAsAction="ifRoom"/>
    <item android:id="@+id/action_share"
          android:title="@string/keyboard_menu_share"
          android:icon="@mipmap/ic_share_white_24dp"
          app:showAsAction="ifRoom"/>

</menu>